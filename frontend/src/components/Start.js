import React from 'react'
import Button from 'react-bootstrap/Button';

function Start() {
  return (
    <div>
     <Button variant="primary" href={'/UploadCode'}>Primary</Button>
    </div>
  )
}

export default Start
